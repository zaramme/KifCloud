# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

module:testrunner

GET     /                       App.Index

GET 	/board/:code			App.Index
GET 	/board/:code/:move      App.Index

#GET     /uploader				App.Submit
#GET 	/textloader				Utils.TextLoader

GET 	/about					App.About
GET 	/release				App.Release

#GET 	/login					Auth.Login
#GET 	/token 					Auth.GetToken
# API Endpoints
GET 	/api/board/				Board.Init	
GET		/api/board/:code		Board.getBoard
GET   	/api/board/:code/:move	Board.getBoardWithMove
#GET 	/api/description/:code  Panel.GetDescription
#PUT		/api/description/:code  Panel.PutDescription
POST	/api/kifu/directTextLoader		Kifu.LoadText
#GET 	/api/kifu/count/		Kifu.GetKifuCount
#GET     /api/kifu/info/userID/:userID	Kifu.GetKifuInfoList
#GET 	/api/kifu/:kifuID		Kifu.GetKifu

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all
*       /:controller/:action                    :controller.:action
